import { Options, Schema } from '../shared/params';
import { TargetConfiguration, WorkspaceConfiguration } from '../shared/workspace';
export interface RunOptions {
    project: string;
    target: string;
    configuration: string;
    help: boolean;
    runOptions: Options;
}
export declare function printRunHelp(opts: RunOptions, schema: Schema): void;
export declare function validateTargetAndConfiguration(workspace: WorkspaceConfiguration, opts: RunOptions): void;
export interface TargetContext {
    root: string;
    target: TargetConfiguration;
    workspace: WorkspaceConfiguration;
    projectName: string;
}
export declare function run(cwd: string, root: string, args: string[], isVerbose: boolean): Promise<any>;
